import 'package:flutter/material.dart';

class PurchaseInvoices extends StatelessWidget {
  const PurchaseInvoices({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Purchase Invoices'),);
  }
}
