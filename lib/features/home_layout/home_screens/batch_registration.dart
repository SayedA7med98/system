import 'package:flutter/material.dart';

class BatchRegistration extends StatelessWidget {
  const BatchRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Batch Registration'),);
  }
}
