import 'package:flutter/material.dart';

class SalesPurchaseReports extends StatelessWidget {
  const SalesPurchaseReports({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Sales Purchase Reports'),);
  }
}
