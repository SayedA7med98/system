import 'package:flutter/material.dart';

class InventoryReports extends StatelessWidget {
  const InventoryReports({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Inventory Reports'),);
  }
}
